<Window x:Class="AxcToAzure.View.InstructionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AxcToAzure.View"
                      xmlns:vm="clr-namespace:AxcToAzure.ViewModel" 
        d:DataContext="{d:DesignInstance vm:InstructionsViewModel, IsDesignTimeCreatable=False}"
        mc:Ignorable="d"
        Title="InstructionView" WindowState="Maximized" Height="800" Width="1200" FontSize="22">
    <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="3*"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Image Grid.Row="0" Margin="15"  VerticalAlignment="Top" Source="{Binding Source}"/>
    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <TextBlock Grid.Column="0" Text="{Binding Column1}" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="10"/>

      <TextBlock Grid.Column="1" Text="{Binding Column2}" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="10"/>
       
    </Grid>
  </Grid>
</Window>
